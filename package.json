{
  "name": "gamecalendar-clone",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "snapshot:data": "node scripts/generate-static-data.mjs",
    "dev": "npm run dev --workspace frontend -- --host 127.0.0.1",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run snapshot:data && npm run build --workspace frontend",
    "start": "npm run snapshot:data && npm run dev --workspace frontend -- --host 127.0.0.1"
  }
}
